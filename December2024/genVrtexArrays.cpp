#include <iostream>

//genVetexArrays(int numOfArrays, int* array)

int main(int argc, const char* argv) {
    
    int numberOfVertexArrays = 3;
    int arrays[3];
    
    genVetexArrays(3, arrays)  // generates vertex arrays, number of arrays ->into the array of ints.
    
    return 0;
}
