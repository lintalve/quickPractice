#include "stdio.h"

enum notes {
    DO,
    RE,
    MI,
    FA
    SOL,
    LA,
    SI
    
};

void playNote() {
    
}

int main() {
    int anInt {2};
    
    enum notes note;
    
    switch(note) {
        case DO:
            printf("Number DO", );
            break;
        case RE:
            printf("Number is ");
            break;
        case
    }
}
